# Enable Journal
storage:
   journal:
      enabled: true
   dbPath: ../database/SeireiGakko/data/data02

# System Log Settings
#systemLog:
   #destination: file
   #path: "../database/SeireiGakko/log/mongo-server02.log"
   #logAppend: true

# Network Settings
net:
   bindIp: 127.0.0.1
   port: 27018

#Authorization Settings
security:
   authorization: disabled
   #keyFile: "openssl rand -base64 741 > mongodb-keyfile chmod 600 mongodb-keyfile"

# Replication
replication: 
   replSetName: "GakkoRS"